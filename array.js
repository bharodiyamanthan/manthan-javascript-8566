
let data = [11,22,33,44,55];


// push method = (add data to last index)

// data.push(66,77,88);

// pop method = (remove data to last index)

// data.pop()
// data.pop()
// data.pop()

// shift method = (remove data to first index)

// data.shift()
// data.shift()

// unshift method = (add data to first index) 

// data.unshift(99,78,44,34);

// splice method = add or remove any position

// data.splice(2,3)


console.log(data);